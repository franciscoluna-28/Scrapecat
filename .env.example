# === MANDATORY (Demo / Production) ===
# Get a free key at https://openrouter.ai/keys
# Used by /api/demo/generate-report and /api/demo/reports/[id]/reply
OPENROUTER_API_KEY=your_openrouter_api_key

# === OPTIONAL (Demo / Production) ===
# Without this, unauthenticated GitHub requests are limited to 60/hr.
# With a token: 5000/hr. Get one at https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token

# === FULL APP ONLY (not used by /demo routes) ===
# These are only needed if you also run the original full-featured app.
# GROQ_API_KEY=your_groq_api_key
# DATABASE_URL=file:data/dev.db
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# R2_ACCESS_KEY_ID=your_r2_access_key
# R2_SECRET_ACCESS_KEY=your_r2_secret
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# R2_BUCKET_NAME=reports
# R2_PUBLIC_URL=https://<your-custom-domain>.com/reports
